// Methods On Objects.
// observe the coding patterns in the game.

// player is an Object
// create a function inside of Object, something which we also call as Method
let player = {
  name: "Jane",
  chips: 145,
  sayHello: function () {
    console.log("Hello World!");
  },
};

// invoke the function inside the Object
player.sayHello(); // Hello World!

// TL;DR
// a variable - a dot - a function that we calling - with the parentheses at the end. 👇
document.getElementById("message-element");
document.getElementById("sum-element");
document.getElementById("card-element");
document.getElementById("player-el");

// x
playerEl.textContent = player.name + ": £" + player.chips;

// Object with the Method
console.log(cards); // to check if the card given earlier

// Math is an Object, random() is a function in that Object also called a Method.
Math.floor(Math.random() * 13) + 1;

// Method is a function that attached to an Object
