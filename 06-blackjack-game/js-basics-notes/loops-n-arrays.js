let messages = [
  "Hey, how's it going?",
  "I'm great, thank you! How about you?",
  "All good. Been working on my portfolio lately.",
  "Same here!",
  "Great to hear",
  "🙌",
];

// want the array `messages` to be in its own lines.
// make the `i` as an index instead of count
// to get all array n make it more dynamic -- use `array.length` instead
// i += 1 can be used also as `i++`
for (let i = 0; i < messages.length; i++) {
  console.log(messages[i]);
}
